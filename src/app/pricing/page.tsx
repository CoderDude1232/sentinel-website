import { RoutePlaceholder } from "@/components/route-placeholder";

export default function PricingPage() {
  return (
    <RoutePlaceholder
      title="Pricing"
      description="Pricing tiers will be defined after the first production launch cycle."
    />
  );
}

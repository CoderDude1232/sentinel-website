import { RoutePlaceholder } from "@/components/route-placeholder";

export default function FeaturesPage() {
  return (
    <RoutePlaceholder
      title="Features"
      description="Feature overview for Sentinel modules: Moderation, ER:LC Integration, Activity, Infractions, Sessions, and Departments."
    />
  );
}

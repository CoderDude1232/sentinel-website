import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ContactPage() {
  return (
    <RoutePlaceholder
      title="Contact"
      description="Contact page placeholder for support intake and enterprise requests."
    />
  );
}

import { RoutePlaceholder } from "@/components/route-placeholder";

export default function LoginPage() {
  return (
    <RoutePlaceholder
      title="Login"
      description="Discord OAuth login page placeholder. Additional providers will be added later through the shared auth provider layer."
    />
  );
}
